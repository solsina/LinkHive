{
  "name": "@linkhive/config",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo 'Config package - no build needed'",
    "dev": "echo 'Config package - no dev needed'",
    "lint": "echo 'Config package - no lint needed'",
    "clean": "echo 'Config package - no clean needed'"
  },
  "devDependencies": {
    "typescript": "^5.3.2"
  }
}
